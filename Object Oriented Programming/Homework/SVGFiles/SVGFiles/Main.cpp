#include <iostream>
#include "constants.h"

int main() {

	while (true)
	{
		char input[INPUT_BUFFER];

		if (strcmp(input, SAVE_COMMAND) == 0)
		{

		}
		else if (strcmp(input, EXIT_COMMAND) == 0) {
			break;
		}
	}
}